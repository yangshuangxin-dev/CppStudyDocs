# Docker和K8S

## Docker是什么

1. docker是一款可用于构建容器化平台的软件。
2. docker是一款进程管理软件。
3. docker主要用于部署应用程序并守护应用程序。
4. 容器是由docker软件启动的业务集成，该进程由docker全权管理。
5. 容器时进程，由内核机制来完成容器隔离。
6. docker软件包包含客户端(docker cli)和服务端（docker server）,本机客户端和服务端通过Unix套接字通信。
7. docker服务端通过接收客户端请求来管理容器进程和镜像。

## K8S是什么

1. k8s 是一个分布式容器管理（编排）与服务管理平台（软件）。
2. k8s 本身不具备将应用容器化的能力，而是通过容器运行时接口与容器化平台交互，从而管理容器（crud）。
3. k8s协调多个节点以及资源，确保应用程序的稳定运行。
4. k8s master节点组件有：调度器、控制管理器、api-server、etcd。
5. k8s worker节点组件有：
    - kubectl k8s命令行客户端，用于与api server交互
    - kubelet 每个k8s节点机器上运行的守护进程，用于获取节点信息、节点容器信息以及负责与节点容器平台交互。
    - kube-proxy 实现网络代理和负载均衡
    - 容器运行时（cri）与kubelet和容器平台交互
    - 容器平台（docker）负责具体地方容器操作

## Docker能做什么

1. 应用程序打包（应用程序+依赖环境）解决环境一致性问题。
2. 进程（容器）隔离以及进程的资源限制。
3. docker swarm 可以应用程序快速部署、快速伸缩。
4. docker swarm 可以实现应用程序的负责均衡与集群化部署。
5. docker swarm 可以应用配置与密钥管理。

## K8S能做什么

1. 快速部署分布式应用，并提供自动伸缩机制和回滚机制。
2. 提供服务发现和负载均衡。
3. 容器配额管理。
4. 故障发现和故障转移。
5. 提供pod作为容器的载体，可以方便实现sidecar模式（一个主业务容器+一个或多个辅助容器）
6. 应用配置和密钥管理。
7. 节点的容器信息与资源的监控。
8. 支持多种存储类型。

## Docker的技术边界

1. 遵循单一原则，原则一个docker只运行一个应用程序主程序。
2. docker是进程，不能在容器中搭建带界面的开发环境。

## K8S的技术边界

1. k8s本身不具备将应用程序容器化的能力
2. 不能部署源码，不构建镜像。
3. 不限定应用程序类型。
4. 不支持应用程序级别的内置服务（mysql、kafka等中间件）。

## 为什么有docker swarm还需要k8s

1. docker swarm是docker原生的容器编排工具，只支持docker。
2. k8s支持所有实现了cri 的容器平台。
3. docker swarm适用于复杂性要求不高，且规模较小的场景。swarm的资源类型较少，没有基于容器封装出更多的资源类型。没有资源配置管理，当部署应用或服务后，必须人工管理部署指令和更新指令。没有命名空间，不适合多租户场景。
4. k8s适用于比较复杂、规模较大的场景。基于pod封装了更多的资源类型，可以根据不同的需求使用不同的资源类型。所有的资源军可以定义为yaml或者json格式，资源被应用后存储到etcd，资源配置无需人工管理。提供了命名空间，适用于多租户与多业务线场景。
5. docker swarm可扩展能力：网络插件与存储插件
6. k8s 可扩展能力：网络插件、存储插件、容器插件、容器运行时、调度器、聚合api、自定义资源等。

## K8S的相关概念

1. pod是k8s的最小调度单元，每个pod包含一个跟容器（pause容器）和一个或多个业务容器。
2. service是将一组运行在pod中的应用程序公开为网络服务。
3. 控制器有deployment、DaemonSet等均为资源控制器，其作用是使集群中的一组pod的实际状态向控制器所定义的预期状态靠拢。
4. label 是键值对结构，键和值均由用户定义，可以被附加到各种资源上，用于资源管理。

## K8S的扩展

1. 网络插件：k8s本身不提供内置的网络插件，提供CNI容器网络接口。
2. 存储插件：k8s本身不提供内置存储，提供CSI容器存储接口。
3. ingress controller：使用网络七层（应用层）负载。
4. 扩展k8s api：api server 、自定义资源（crd）

## K8S的api扩展使用条件

1. 现有的内置资源无法满足需求的时候可以考虑扩展api。
2. 需要支持k8s api特性，即需要使用一些内置资源。
3. 可以接受k8s对REST资源路径所作的限制。
4. 新的资源api属于声明式api。

## K8S的api server与crd选择

1. 能使用自定义资源解决问题，那么有限使用自定义资源。
2. 非商用产品、对象较小、自动较少、内部使用等使用crd。
3. k8s资源都是存储在内置etcd中，如果资源对象需要存储在其他位置，则使用api server。
4. api server需要额外部署api服务器。