# Kubernetes(k8s)的安装升级

安装K8S的集群环境，基本的系统要求如下：

-  一台或多台运行兼容 deb/rpm 的 Linux 操作系统的计算机；例如：Ubuntu 或 CentOS。
- 每台机器 2 GB 以上的内存，内存不足时应用会受限制。
- 用作控制平面节点的计算机上至少有2个 CPU。
- 集群中所有计算机之间具有完全的网络连接。你可以使用公共网络或专用网络。

## Docker容器运行时安装

使用`cat /proc/version`查看内核信息，然后从源码地址[cri-dockerd · GitHub](https://github.com/Mirantis/cri-dockerd/releases),根据内核信息选择对应的文件下载地址，进行下载和安装使用：

```shell
# 下载
wget https://github.com/Mirantis/cri-dockerd/releases/download/v0.3.1/cri-dockerd-0.3.1.amd64.tgz
 # 解压到/tmp目录
tar -zxvf cri-dockerd-0.3.1.amd64.tgz -C /tmp
 # 安装
sudo cp /tmp/cri-dockerd/cri-dockerd /usr/bin/
```

如果找不到需要的版本，可以通过docker来构建：

```shell
# 下载代码
git clone https://github.com/Mirantis/cri-dockerd.git
 # 进入目录
cd cri-dockerd
 # 查看tag
 git tag
 # 选择需要的tag
 git checkout v0.3.1
```

创建Dockerfile文件，构建镜像：

```dockerfile
FROM golang:1.18
ADD ./ /go/src/cri-dockerd
WORKDIR /go/src/cri-dockerd
RUN go env -w GOPROXY=https://proxy.golang.com.cn,https://goproxy.cn,direct
RUN go build -o cri-dockerd
```

进行镜像的构建：

```shell
# 构建镜像
docker build -t cri-dockerd:latest  .
 # 运行容器
docker run --rm -d -it --name cri-dockerd cri-dockerd:latest bash
 # 将容器中编译好的二进制文件拷贝的宿主机
docker cp cri-dockerd:/go/src/cri-dockerd/cri-dockerd ./
 # 将二进制文件安装到/usr/bin/
 sudo cp cri-dockerd /usr/bin/
```

### cri-dockerd服务配置和启动

获取配置文件：

```shell
git clone https://github.com/Mirantis/cri-dockerd.git
 # 将源中systemd配置文件拷贝到相应目录
sudo cp cri-dockerd/packaging/systemd/*  /etc/systemd/system/
```

文件 /etc/systemd/system/cri-docker.socket 内容无需修改。

文件 /etc/systemd/system/cri-docker.service Service.ExecStart 字段增加 --network-plugin 与pod-infra-container-image 选项。

--pod-infra-container-image 镜像tag需要根据具体k8s安装版本指定。

```shell
 [Service]
Type=notify
ExecStart=/usr/bin/cri-dockerd --container-runtime-endpoint fd:// --network-plugin=cni \
--pod-infra-container-image=registry.aliyuncs.com/google_containers/pause:3.7
ExecReload=/bin/kill -s HUP $MAINPID
TimeoutSec=0
RestartSec=2
Restart=always
```

 启动服务容器运行时服务：

```shell
# 重新加载配置
sudo systemctl daemon-reload
 # 设置为开机自启动
sudo systemctl enable cri-docker
 # 启动服务
sudo systemctl start cri-docker
 # 检查服务状态
sudo systemctl status cri-docker
```

## kubeadm和相关工具安装

kubeadm是Kubernetes从1.4版本开始引入的命令行工具，致力于简化集群的安装过程，并解决 Kubernetes集群高可用问题。

```shell
# 更新包管理器索引，并安装相关软件支持HTTPS访问
sudo apt-get update
sudo apt-get install -y apt-transport-https ca-certificates curl

# 下载gpg秘钥, 使用此方式获取gpg key,需要下载之后，在本地用gpg加密
curl -fsSL https://mirrors.aliyun.com/kubernetes/apt/doc/apt-key.gpg | sudo gpg --dearmor -o /usr/share/keyrings/kubernetes-archive-keyring.gpg

# 设置Kubernetes镜像源
echo "deb [signed-by=/usr/share/keyrings/kubernetes-archive-keyring.gpg] https://mirrors.aliyun.com/kubernetes/apt/ kubernetes-xenial main" | sudo tee /etc/apt/sources.list.d/kubernetes.list

# 更新apt软件索引，并查看相关软件的可用版本
sudo apt-get update
apt-cache madison kubelet kubeadm kubectl

# 安装特定版本
sudo apt-get install -y kubelet=<VERSION_STRING> kubeadm=<VERSION_STRING> kubectl=<VERSION_STRING>
sudo apt-get install -y kubelet=1.24.1-00 kubeadm=1.24.1-00 kubectl=1.24.1-00

# 安装最新版本
sudo apt-get install -y kubelet kubeadm kubectl

# 锁定软件版本,防止自动升级更新
sudo apt-mark hold kubelet kubeadm kubectl

# 检查kubelet状态
systemctl status kubelet

# 卸载
sudo apt-get remove kubelet kubectl kubeadm
```

> kubelet在刚安装完成时，会处于一个自动启动状态，每10s启动一次，在没有完成初始化之前它一致处于这种状态，所以不要纠结于kubelet安装之后没有启动。

## 主机环境配置

由于Kubernetes中kubelet组件默认使用的cgroup  driver 为 “systemd”，所以需要将docker的cgroup  driver也修改为 “systemd”。

```shell
# 修改节点上Docker 的cgroup driver为systemd
# 打开docker配置文件进行修改，该文件默认情况下不存在，可以新建一个，不同操作系统的文件位置可能不一样
sudo vim /etc/docker/daemon.json
 # 增加配置内容
"exec-opts": [
 "native.cgroupdriver=systemd"
 ]
 
 # 重新加载配置并重启服务 
sudo systemctl daemon-reload
sudo systemctl restart docker
```

关闭防火墙、Selinux、swap

```shell
# 关闭防火墙
sudo systemctl stop firewalld
sudo systemctl disable firewalld

# selinux，这个是用来加强安全性的一个组件，但非常容易出错且难以定位，一般上来装完系统就先给禁用了
# 查看 selinux 状态
sudo apt install selinux-utils getenforce
 # 禁用 
sudo setenforce 0

# 禁用swap
# 查看交换区
free
# 禁用交换区
sudo swapoff -a
# 打开文件注释交换区定义
/etc/fstab
```

修改主机名

```shell
# /etc/hosts文件增加主机名与本机ip映射
127.0.1.1 k8s-master1
# 修改主机名
sudo hostnamectl set-hostname k8s-master1
# 查看主机名
hostname
```

## 初始化master

生成默认配置文件：

```shell
kubeadm config print init-defaults > init.default.yaml
```

修改配置文件：

```shell
# 修改地址 节点IP地址
localAPIEndpoint.advertiseAddress: 192.168.239.142
# 修改套接字
nodeRegistration.criSocket: unix:///var/run/cri-dockerd.sock
# 修改节点名称
nodeRegistration.name: k8s-master1
# 修改镜像仓库地址为国内开源镜像库
imageRepository: registry.aliyuncs.com/google_containers
# 修改版本号
kubernetesVersion: 1.24.1
# 增加podSubnet，由于后续会安装flannel 网络插件，该插件必须在集群初始化时指定pod地址
# 10.244.0.0/16 为flannel组件podSubnet默认值，集群配置与网络组件中的配置需保持一致
networking.podSubnet: 10.244.0.0/16
```

拉取相关镜像：

```shell
sudo kubeadm config images pull --config=init.default.yaml
```

初始化集群：

```shell
# 通过配置文件初始化，建议选择配置文件初始化
sudo kubeadm init --config=init.default.yaml
 # 通过参数初始化
kubeadm init --image-repository registry.aliyuncs.com/google_containers --kubernetes-version=v1.24.1 --pod-network-cidr=10.244.0.0/16 --apiserver-advertise-address=192.168.239.142 --cri-socket unix:///var/run/cri-dockerd.sock
```

当前用户为普通用户，请执行以下指令：

```shell
mkdir -p $HOME/.kube
sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config
```

当前用户为root用户，请配置环境变量：

```shell
# /etc/profile 末尾添加环境变量
export KUBECONFIG=/etc/kubernetes/admin.conf
 # 执行命令，立即生效
source /etc/profile
```

> 以上两种情况二选一

添加节点到集群命令，注意：需要指定 --cri-socket unix:///var/run/cri-dockerd.sock

```shell
# 如果过期了，在master节点上执行以下命令可重新获得
sudo kubeadm token create --print-join-command
# 添加节点到集群指令
kubeadm join 192.168.239.146:6443 --token abcdef.0123456789abcdef --discovery-token-ca-cert-hash  --cri-socket unix:///var/run/cri-dockerd.sock
```

查看集群状态:

```shell
# 查看所有pod
 sudo kubectl get pods --all-namespaces
 # 查看节点
sudo kubectl get node
 # 查看所有组件
sudo kubectl get cs
```

### 添加pod网络组件

下载flannel网络组件定义文件到本地，命名为kube-flannel.yml（复制文件内容粘贴到本地文件也可以）

`https://github.com/flannel-io/flannel/blob/master/Documentation/kube-flannel.yml`

```shell
kubectl apply -f kube-flannel.yml

# 或者直接安装网络插件
kubectl apply -f https://raw.githubusercontent.com/flannel-io/flannel/master/Documentation/kube-flannel.yml
```

开启kube-proxy的ipvs模式：

```shell
# 修改mod
 kubectl edit -n kube-system cm kube-proxy
# 修改：mode: "ipvs"
# 重启kube-proxy 守护进程
kubectl rollout restart -n kube-system daemonset kube-proxy 
# 查看集群状态：
kubectl get pod --all-namespaces 
kubectl get pod -A
```

到此，完成master节点的初始化，若有pod状态不正常，可通过查看日志获取错误信息：

```shell
kubectl -n kube-system logs <pod>
```

##  Node节点初始化

node节点和master节点一样，首先需要环境安装：

1. 安装docker
2. 安装cri-dockerd 运行时，并启动服务
3. kubeadm、kubelet、kubectl等工具安装

然后进行节点环境的修改：

1. 修改docker的Cgroup Driver
2. 关闭防火墙
3. 禁用selinux
4.  禁用swap
5. 调整主机名（若需要）

将节点添加到集群：

```shell
# 在master节点上执行以下命令可获得
sudo kubeadm token create --print-join-command
# 通过以上命令获得加入到集群的命令后，
# 在命令上附加参数 --cri-socket unix:///var/run/cri-dockerd.sock
```

重置节点：

```shell
sudo kubeadm reset
# 或
sudo kubeadm reset --cri-socket unix:///var/run/cri-dockerd.sock

sudo rm -rf /var/lib/kubelet /var/lib/dockershim /var/run/kubernetes /var/lib/cni /etc/cni/net.d $HOME/.kube/config
 
sudo ipvsadm --clear
# 删除网络
sudo ifconfig cni0 down
sudo ip link delete cni0
```

## K8S进行集群升级

查看当前集群的配置：

```shell
# 查看节点上运行的pod
kubectl get pod -o wide |grep <nodename>
# 查看集群配置文件
kubectl -n kube-system get cm kubeadm-config -o yaml
# 查看当前集群节点
kubectl get node 
```

### 升级master节点

进行软件包管理软件更新：

```shell
# 更新包管理器
sudo apt-get update
# 查看可用版本
apt-cache madison kubeadm
# 解除 kubeadm软件包保留状态
sudo apt-mark unhold kubeadm
# 安装
sudo apt-get install -y kubeadm=1.25.5-00
# 设置为保留，即不自动更新
sudo apt-mark hold kubeadm
 # 验证版本
kubeadm version
```

检查可升级到哪些版本，并验证你当前的集群是否可升级，然后进行升级：

```shell
# 验证升级计划
sudo kubeadm upgrade plan

# 显示哪些差异将被应用于现有的静态 pod 资源清单
sudo kubeadm upgrade diff 1.25.5

# master节点升级，升级到 1.25.5版本，此命令仅升级master节点（control plane）
sudo kubeadm upgrade apply v1.25.5

# 腾空节点，即将节点上除守护进程之外的其他进程调度到其他节点，同时将开启调度保护
kubectl drain <nodename> --ignore-daemonsets

# 升级kubelet与kubectl组件
sudo apt-mark unhold kubelet kubectl
sudo apt-get install -y kubelet=1.25.5-00 kubectl=1.25.5-00
sudo apt-mark hold kubelet kubectl

# 重启 kubelet
 sudo systemctl daemon-reload
 sudo systemctl restart kubelet
 
 # 解除调度保护
 kubectl uncordon <nodename>
```

### 升级工作节点

进行软件包管理软件更新：

```shell
# 更新包管理器
sudo apt-get update
# 解除 kubeadm软件包保留状态
sudo apt-mark unhold kubeadm
 # 安装
sudo apt-get install -y kubeadm=1.25.5-00
 # 设置为保留，即不自动更新
sudo apt-mark hold kubeadm
 # 验证版本
kubeadm version
```

节点进行升级：

```shell
# 升级节点kubelet 配置
sudo kubeadm upgrade node

# 腾空节点，同时开启调度保护，此命令请在master节点操作
kubectl drain <nodename> --ignore-daemonsets

# 升级kubelet与kubectl组件
sudo apt-mark unhold kubelet kubectl
sudo apt-get install -y kubelet=1.25.5-00 kubectl=1.25.5-00
sudo apt-mark hold kubelet kubectl

# 重启 kubelet
 sudo systemctl daemon-reload
 sudo systemctl restart kubelet
 
 # 解除调度保护，master节点上执行该命令
 kubectl uncordon <nodename>
 
 # 查看集群状态
 kebectl get node
```

